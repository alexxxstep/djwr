"""
Factory Boy factories for creating test data.

Factories will be created in Phase 2 when models are implemented.
"""
# import factory
# from app.models import User, City, Subscription, WeatherData, NotificationLog

# Example factory structure (to be implemented in Phase 2):
# class UserFactory(factory.django.DjangoModelFactory):
#     class Meta:
#         model = User
#
#     email = factory.Sequence(lambda n: f"user{n}@example.com")
#     username = factory.Sequence(lambda n: f"user{n}")
#     is_email_verified = True

